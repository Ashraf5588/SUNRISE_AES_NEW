<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
<link rel="manifest" href="/site.webmanifest">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>NEPALI Project Evaluation Form</title>
  <style>
    :root {
      --primary-color: #003366;
      --secondary-color: #ff6600;
      --bg-color: #f4f7f6;
      --card-bg: #ffffff;
      --text-color: #333;
      --border-color: #e0e0e0;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background-color: var(--bg-color);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      color: var(--text-color);
      line-height: 1.6;
      padding: 20px;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
      background: var(--card-bg);
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
      overflow: hidden;
    }

    .header {
      background: linear-gradient(135deg, var(--primary-color), #004080);
      color: white;
      padding: 25px;
      text-align: center;
    }

    .header h1 {
      font-size: 28px;
      margin-bottom: 10px;
      font-weight: 700;
    }

    .header p {
      font-size: 16px;
      opacity: 0.9;
    }

    .form-content {
      padding: 30px;
    }

    .section {
      background: #fff;
      border: 1px solid var(--border-color);
      border-radius: 12px;
      margin: 20px 0;
      overflow: hidden;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    }

    .section-header {
      background-color: var(--primary-color);
      color: white;
      padding: 15px 20px;
      font-size: 18px;
      font-weight: 600;
    }

    .section-content {
      padding: 20px;
    }

    .form-row {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 20px;
    }

    .form-group {
      display: flex;
      flex-direction: column;
    }

    .form-group label {
      font-weight: 600;
      margin-bottom: 8px;
      color: var(--primary-color);
      font-size: 14px;
    }

    .form-control {
      padding: 12px 15px;
      border: 1px solid var(--border-color);
      border-radius: 8px;
      font-size: 16px;
      transition: all 0.3s ease;
      background: white;
    }

    .form-control:focus {
      outline: none;
      border-color: var(--secondary-color);
      box-shadow: 0 0 0 3px rgba(255, 102, 0, 0.1);
    }

    .form-control:readonly {
      background: #f8f9fa;
      color: #6c757d;
    }

    .evaluation-table, table {
      width: 100%;
      border-collapse: collapse;
      margin: 15px 0;
      background: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      border: 1px solid var(--border-color);
    }

    .evaluation-table th, table th {
      background-color: var(--primary-color);
      color: white;
      padding: 15px 12px;
      text-align: left;
      font-weight: 600;
      font-size: 16px;
      border-bottom: 2px solid #002244;
    }

    .evaluation-table td, table td {
      padding: 12px 15px;
      border: 1px solid var(--border-color);
      vertical-align: middle;
      font-size: 15px;
      line-height: 1.6;
      color: black;
    }

    .evaluation-table tr:hover, table tr:hover {
      background-color: #fff8e1;
    }

    .evaluation-table tr:nth-child(even), table tr:nth-child(even) {
      background-color: #f8f9fa;
    }

    .evaluation-table td:last-child, table td:last-child {
      text-align: center;
    }

    .radio-group {
      display: flex;
      align-items: center;
      gap: 8px;
      margin: 8px 0;
      justify-content: center;
      flex-wrap: wrap;
    }

    .radio-group input[type="radio"] {
      display: none;
    }

    .radio-group label {
      margin: 0;
      font-weight: 600;
      cursor: pointer;
      font-size: 14px;
      color: #555;
      padding: 8px 16px;
      border-radius: 20px;
      transition: all 0.2s ease;
      background: #f0f0f0;
      border: 1px solid #ddd;
    }

    .radio-group label:hover {
      background: #e0e0e0;
    }

    .radio-group input[type="radio"]:checked + label {
      background-color: var(--secondary-color);
      color: white;
      border-color: var(--secondary-color);
      transform: scale(1.05);
      box-shadow: 0 2px 5px rgba(0,0,0,0.2);
    }

    .mark-input-wrapper {
      display: flex;
      align-items: center;
      gap: 8px;
      justify-content: center;
      flex-wrap: wrap;
    }

    .mark-input-wrapper .mark-input {
      width: 80px;
      padding: 6px 8px;
      border: 2px solid var(--border-color);
      border-radius: 4px;
      font-size: 14px;
      text-align: center;
      transition: all 0.3s ease;
    }

    .mark-input-wrapper .mark-input:focus {
      outline: none;
      border-color: var(--secondary-color);
      box-shadow: 0 0 0 3px rgba(255, 102, 0, 0.1);
    }

    .mark-input-wrapper .mark-input.has-value {
      border-color: var(--secondary-color);
      background-color: #fff8e1;
    }

    .mark-input-wrapper .mark-input.saved-value {
      border-color: #28a745;
      background-color: #e8f5e9;
    }

    .unit-section {
      background: white;
      border: 1px solid var(--border-color);
      border-radius: 10px;
      margin: 20px 0;
      overflow: hidden;
    }

    .unit-header {
      background-color: #e6f0ff;
      color: var(--primary-color);
      padding: 12px 15px;
      font-weight: 700;
      font-size: 18px;
      border-bottom: 1px solid var(--primary-color);
    }

    .unit-content {
      padding: 20px;
    }

    .practical-section {
      background: #fafcff;
      border: 1px solid var(--border-color);
      border-radius: 8px;
      margin: 15px 0;
      overflow: hidden;
    }

    .practical-header {
      background-color: var(--primary-color);
      color: white;
      padding: 10px 15px;
      font-weight: 600;
    }

    .practical-content {
      padding: 15px;
    }

    .project-section {
      background: #fff8e1;
      border: 1px solid var(--border-color);
      border-radius: 8px;
      margin: 15px 0;
      overflow: hidden;
      color: black !important; 
    }

    .project-header {
      background-color: #fff3e0;
      color: var(--secondary-color);
      padding: 10px 15px;
      font-weight: 600;
      border-left: 5px solid var(--secondary-color);
    }

    .project-content {
      padding: 15px;
    }

    .total-display {
      background-color: var(--primary-color);
      color: white;
      padding: 15px;
      border-radius: 8px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin: 20px 0;
      font-weight: bold;
    }

    .total-display label {
      font-size: 16px;
      color: white;
    }
    
    .total-display input {
        background: rgba(255,255,255,0.2);
        border: 1px solid rgba(255,255,255,0.3);
        color: white;
        padding: 5px 10px;
        border-radius: 4px;
        width: 80px;
        text-align: center;
        font-weight: bold;
    }

    .submit-btn {
      background: linear-gradient(45deg, var(--primary-color), #004080);
      color: white;
      border: none;
      padding: 15px 30px;
      font-size: 18px;
      font-weight: 700;
      border-radius: 10px;
      cursor: pointer;
      transition: all 0.3s ease;
      width: 100%;
      margin-top: 30px;
      text-transform: uppercase;
      letter-spacing: 1px;
      box-shadow: 0 4px 15px rgba(0, 51, 102, 0.3);
    }

    .submit-btn:hover {
      background: linear-gradient(45deg, var(--secondary-color), #ff8533);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(255, 102, 0, 0.4);
    }

    .student-list-container {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
      gap: 10px;
      max-height: 250px;
      overflow-y: auto;
      padding: 15px;
      background: #f8f9fa;
      border: 1px solid var(--border-color);
      border-radius: 8px;
      margin-bottom: 20px;
    }
    
    @media (min-width: 768px) {
        .student-list-container {
            grid-template-columns: repeat(4, 1fr);
        }
    }

    .student-btn {
      background-color: white;
      color: var(--primary-color);
      border: 1px solid var(--primary-color);
      padding: 10px;
      border-radius: 6px;
      cursor: pointer;
      font-weight: 600;
      transition: all 0.2s ease;
      text-align: left;
      font-size: 14px;
      width: 100%;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      display: flex;
      align-items: center;
    }
    
    .student-btn span.roll {
        background: var(--primary-color);
        color: white;
        padding: 2px 6px;
        border-radius: 4px;
        margin-right: 8px;
        font-size: 12px;
        min-width: 25px;
        text-align: center;
    }

    .student-btn:hover {
      background-color: var(--secondary-color);
      color: white;
      border-color: var(--secondary-color);
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    
    .student-btn:hover span.roll {
        background: white;
        color: var(--secondary-color);
    }

    @media (max-width: 768px) {
      .form-row {
        grid-template-columns: 1fr;
      }
      
      .container {
        margin: 10px;
        border-radius: 10px;
      }
      
      .header h1 {
        font-size: 24px;
      }
      
      .form-content {
        padding: 20px;
      }

      .unit-section {
        margin: 15px 0;
      }

      .unit-header {
        padding: 10px 15px;
        font-size: 16px;
      }

      .practical-header, .project-header {
        padding: 8px 12px;
        font-size: 14px;
      }

      .section-header {
        padding: 10px 15px;
        font-size: 16px;
      }

      .section-content {
        padding: 15px;
      }

      .form-control {
        padding: 10px 12px;
        font-size: 16px;
      }

      .total-display {
        padding: 10px;
        margin: 15px 0;
      }

      .total-display label {
        font-size: 14px;
      }

      .table-container {
        overflow-x: auto;
        margin: 15px 0;
        border: 1px solid var(--border-color);
        border-radius: 8px;
        background: white;
        max-width: 100%;
        -webkit-overflow-scrolling: touch;
      }

      .evaluation-table, table {
        min-width: 800px;
        margin: 0;
        border: none;
        box-shadow: none;
      }

      .evaluation-table th, table th {
        padding: 12px 8px;
        font-size: 14px;
        white-space: nowrap;
      }

      .evaluation-table td, table td {
        padding: 10px 8px;
        font-size: 13px;
      }

      .radio-group {
        gap: 6px;
      }

      .radio-group label {
        padding: 5px 10px;
        font-size: 12px;
      }
      
      .student-list-container {
          grid-template-columns: repeat(2, 1fr);
      }
      
      .mark-input-wrapper .mark-input {
        width: 60px;
        font-size: 12px;
        padding: 4px 6px;
      }
    }

    @media (max-width: 480px) {
      body {
        padding: 10px 5px;
      }

      .form-content {
        padding: 10px;
      }

      .header h1 {
        font-size: 20px;
      }

      .student-list-container {
          grid-template-columns: 1fr;
      }
      
      .radio-group label {
        padding: 4px 8px;
        font-size: 11px;
      }
    }
  </style>
</head>
<body>
       <%- include('../partials/mainnav') %>
  <div class="container">
    <div class="header">
      <h1>Nepali Project Evaluation Form</h1>
      <p>Complete evaluation form for practical and project work assessment</p>
    </div>

    <div class="form-content">
      <form action="/practicalprojectform?studentClass=<%= studentClass %>&section=<%= section %>&subject=<%= subject %>&terminal=<%= terminal %>" method="POST">
        
        <!-- Student Information Section -->
        <div class="section">
          <div class="section-header">Student Information</div>
          <div class="section-content">
            <div class="student-list-container">
             <% studentData.forEach((studentname) => { %>
                <button type="button" class="student-btn" id="studentData<%= studentname.roll %>" onclick="fill('<%= studentname.roll %>','<%= studentname.name %>','<%= studentname.reg %>')">
                  <span class="roll"><%=studentname.roll%></span> <%= studentname.name %>
                </button>
              <% }); %>
            </div><input type="hidden" name="reg" >
            <div class="form-row">
              <div class="form-group">
                <label for="roll">Roll Number</label>
                <input type="text" name="roll" id="roll" class="form-control" placeholder="Enter Roll Number" required>
              </div>
              <div class="form-group">
                <label for="name">Student Name</label>
                <input type="text" name="name" id="name" class="form-control" placeholder="Enter Student Name" required>
              </div>
              <div class="form-group">
                <label for="studentClass">Class</label>
                <input type="text" name="studentClass" id="studentClass" class="form-control" value="<%= studentClass %>" readonly>
              </div>
              <div class="form-group">
                <label for="section">Section</label>
                <input type="text" name="section" id="section" class="form-control" value="<%= section %>" readonly>
              </div>
              <div class="form-group">
                <label for="subject">Subject</label>
                <input type="text" name="subject" id="subject" class="form-control" value="<%= subject %>" readonly>
              </div>
              <div class="form-group">
                <label for="terminal">Terminal</label>
                <input type="text" id="terminal" name="terminalName" class="form-control" placeholder="Terminal will be auto-filled" readonly>
              </div>
            </div>
          </div>
        </div>

        <!-- Attendance Section -->
        <div class="section">
          <div class="section-header">Attendance Evaluation</div>
          <div class="section-content">
            <div class="form-row">
              <div class="form-group">
                <label for="attendanceDays">Total Attendance Days</label>
                <input type="number" name="totalAttendance" id="attendanceDays" class="form-control" placeholder="Enter attendance days" oninput="calculateAttendanceMarks()">
              </div>
              <div class="form-group">
                <label for="attendanceMarks">Attendance Marks (Auto-calculated)</label>
                <input type="number" name="attendanceMarks" id="attendanceMarks" class="form-control" readonly step="0.1">
              </div>
            </div>
          </div>
        </div>

        <!-- Participation Section -->
        <div class="section">
          <div class="section-header">Participation Evaluation</div>
          <div class="section-content">
            <div class="table-container">
              <table>
                <thead>
                  <tr>
                    <th>आधार</th>
                    <th>स्तर</th>
                    <th>विवरण</th>
                    <th>अंक</th>
                  </tr>
                </thead>
                <tbody>
                  <tr>
                    <td rowspan="4">सक्रियता र सहभागिता</td>
                    <td>सर्वोत्कृष्ट</td>
                    <td>सिकाइमा जिज्ञासा राख्दै, समूह छलफल र अन्तरक्रियामा सक्रियतापूर्वक भाग लिएको</td>
                    <td><div class="radio-group"><input type="radio" name="participationMarks" value="2" id="part2"> <label for="part2">२</label></div></td>
                  </tr>
                  <tr>
                    <td>उत्कृष्ट</td>
                    <td>सिकाइमा जिज्ञासा राख्दै, समूह छलफल र अन्तरक्रियामा कम सक्रियतापूर्वक भाग लिएको</td>
                    <td><div class="radio-group"><input type="radio" name="participationMarks" value="1.5" id="part1_5"> <label for="part1_5">१.५</label></div></td>
                  </tr>
                  <tr>
                    <td>मध्यम</td>
                    <td>सिकाइमा कम जिज्ञासा राख्दै, समूह छलफल र अन्तरक्रियामा निर्देशानुसार भाग लिएको</td>
                    <td><div class="radio-group"><input type="radio" name="participationMarks" value="1" id="part1"> <label for="part1">१</label></div></td>
                  </tr>
                  <tr>
                    <td>सामान्य</td>
                    <td>सिकाइप्रति उदासीन रहेमा, समूह छलफल र अन्तरक्रियामा सक्रियता नदेखाएको</td>
                    <td><div class="radio-group"><input type="radio" name="participationMarks" value="0.5" id="part0_5"> <label for="part0_5">०.५</label></div></td>
                  </tr>
                </tbody>
              </table>
            </div>
          </div>
        </div>

        <!-- Terminal Marks Section -->
        <div class="section" style="display: <%=terminal==='FINAL'?'none':'block'%>;">
          <div class="section-header">Terminal Marks</div>
          <div class="section-content">
            <div class="form-row">
              <div class="form-group">
                <label for="theoryMarks">Terminal Exam Marks</label>
                <input type="number" name="theoryMarks" id="theoryMarks" class="form-control" placeholder="Enter terminal marks" step="0.1" oninput="calculateTerminalMarks()">
              </div>
              <div class="form-group">
                <label for="obtainedconvertedmarks">Converted Marks (Auto-calculated)</label>
                <input type="number" name="terminalMarks" id="obtainedconvertedmarks" class="form-control" readonly step="0.1">
              </div>
            </div>
          </div>
        </div>

        <!-- Practical/Project Work Section -->
        <div class="section">
          <div class="section-header">
            <% if(studentClass > 8){ %>
              सुनाइ, बोलाइ र पढाइ (१२)
            <% } else { %>
            सुनाइ र बोलाइ (२०)
            <% } %>
          </div>
          <div class="section-content">
            <% ScienceData.forEach((item, itemIdx) => { %>
              <% item.units.forEach((unit, idx) => { %>
                <div class="unit-section">
                  <div class="unit-header">
                    Unit: <%= unit.unitName %>
                    <input type="hidden" name="unit[<%= idx %>][unitName]" value="<%= unit.unitName %>">
                  </div>
                  <div class="unit-content">
                    <% unit.practicals.forEach((practical, prindx) => { %>
                      <!-- Practical <%= prindx + 1 %> -->
                      <div class="practical-section">
                        <div class="practical-header">
                          <%=studentClass>8?'सुनाइ, बोलाइ र पढाइ': 'सुनाइ र बोलाइ' %> <%= prindx + 1 %>: <%= practical %>
                          <input type="hidden" name="unit[<%= idx %>][practicals][<%= prindx %>][practicalName]" value="<%= practical %>">
                        </div>
                        <div class="practical-content">
                          <div class="table-container">
                            <table class="evaluation-table">
                              <thead>
                                <tr>
                                  <th>S.No</th>
                                  <th>मुल्याङ्कनका आधार</th>
                                  <th>परिक्षण पक्ष</th>
                                  <th>सूचक</th>
                                  <th>अंक</th>
                                </tr>
                              </thead>
                              <tbody>




<% let iIndex = 0; %>



<% practicalFormatData.forEach(function(doc){ %>



    <% doc.themes.forEach(function(theme){ %>



        <% 

        let themeRowspan = 0;



        theme.learningOutcome.forEach(function(lo){

            lo.assessmentAspects.forEach(function(aspect){

                aspect.tools.forEach(function(tool){

                    themeRowspan += tool.indicators.length;

                });

            });

        });



        let firstThemeRow = true;

        %>



        <% theme.learningOutcome.forEach(function(lo){ %>



            <%

            let loRowspan = 0;



            lo.assessmentAspects.forEach(function(aspect){

                aspect.tools.forEach(function(tool){

                    loRowspan += tool.indicators.length;

                });

            });



            let firstLoRow = true;

            %>



            <% lo.assessmentAspects.forEach(function(aspect){ %>



                <% aspect.tools.forEach(function(tool){ %>



                    <% tool.indicators.forEach(function(ind, indIndex){ %>



                        <tr>



                            <% if(firstThemeRow){ %>

                                <td rowspan="<%= themeRowspan %>">

                                    <%= iIndex + 1 %>

                                </td>



                                <td rowspan="<%= themeRowspan %>">

                                    <%= theme.themeName %>

                                </td>



                                <% firstThemeRow = false; %>

                            <% } %>



                            <% if(firstLoRow){ %>

                                <td rowspan="<%= loRowspan %>">

                                    <%= lo.learningOutcomeName || "-" %>

                                </td>



                                <% firstLoRow = false; %>

                            <% } %>



                            <td>

                                <%= ind.indicatorName %>

                            </td>



                           <td>
    <div class="radio-group">

        <% for (let j = ind.maxMarks * 2; j >= 1; j--) {
            const mark = (j / 2).toFixed(1).replace(/\.0$/, '');
        %>

            <input
                type="radio"
                class="marks-radio"
                value="<%= mark %>||<%= ind.indicatorName %>||<%= theme.themeName %>"
                name="unit[<%= idx %>][practicals][<%= prindx %>][criteria][<%= iIndex %>]"
                data-practical="<%= idx %>-<%= prindx %>"
                  data-criteria-index="<%= iIndex %>"
                id="practical_<%= idx %>_<%= prindx %>_<%= iIndex %>_<%= ind._id %>_<%= mark.replace('.', '_') %>">

            <label
                for="practical_<%= idx %>_<%= prindx %>_<%= iIndex %>_<%= ind._id %>_<%= mark.replace('.', '_') %>">
                <%= mark %>
            </label>

        <% } %>

    </div>
</td>


                        </tr>



                    <% }) %>



                <% }) %>



            <% }) %>



            <% iIndex++; %>



        <% }) %>



    <% }) %>



<% }) %>




                              </tbody>
                            </table>
                          </div>
                          <div class="total-display">
                            <label>Total Practical Marks</label>
                            <input type="number" name="unit[<%= idx %>][practicals][<%= prindx %>][practicalMarks]" 
                                   value="0" id="practical-total-<%= idx %>-<%= prindx %>" 
                                   class="form-control" readonly>
                          </div>
                        </div>
                      </div>

                      <!-- Project Work <%= prindx + 1 %> -->
                      <div class="project-section">
                        <div class="project-header">
                          <%= studentClass > 8 ? '(ख) सिर्जनात्मक कार्य र परियोजना कार्य तथा प्रस्तुति' : 'पढाई र लेखइ (१६)' %>
                          <%= prindx + 1 %>
                          <input type="hidden" name="unit[<%= idx %>][projectWorks][<%= prindx %>][projectName]" value="">
                        </div>
                        <div class="project-content">
                          <div class="table-container">
                            <table>
                              <thead>
                                <tr>
                                  <th>SN</th>
                                  <th>आधार</th>
                                  <th>पक्ष</th>
                                  <th>सूचक</th>
                                  <th>अंक</th>
                                </tr>
                              </thead>
                              <tbody>
                                <% projectFormatData.forEach(function(doc) { %>
                                  <% doc.themes.forEach(function(theme) { %>
                                    <% let themeRowspan = theme.learningOutcome.reduce((sum, lo) => sum + lo.indicators.length, 0); %>
                                    <% let firstThemeRow = true; %>

                                    <% theme.learningOutcome.forEach(function(lo,loIdx) { %>
                                      <% let loRowspan = lo.indicators.length; %>

                                      <% lo.indicators.forEach(function(ind, indIndex) { %>
                                        <tr>
                                          <% if(firstThemeRow && indIndex === 0) { %>
                                            <td rowspan="<%= themeRowspan %>"><%= iIndex+1 %></td>
                                            <td rowspan="<%= themeRowspan %>"><%= theme.themeName %></td>
                                            <% firstThemeRow = false; %>
                                          <% } %>

                                          <% if(indIndex === 0) { %>
                                            <td rowspan="<%= loRowspan %>"><%= lo.learningOutcomeName %></td>
                                          <% } %>

                                          <td><%= ind.indicatorName %></td>
                                          <td>
    <div class="mark-input-wrapper">
        <div class="radio-group">
            <% for (let j = ind.indicatorsMarks * 2; j >= 1; j--) { 
                   const mark = (j / 2).toFixed(1).replace(/\.0$/, '');
            %>

                <input type="radio"
                       value="<%= mark %>||<%= ind.indicatorName %>||<%= lo.learningOutcomeName %>"
                       class="marks-radio-project"
                       name="unit[<%= idx %>][projectWorks][<%= prindx %>][criteria][<%= iIndex %>]"
                       data-project="<%= idx %>-<%= prindx %>"
                         data-criteria-index="<%= iIndex %>"
                       data-input-id="project_input_<%= idx %>_<%= prindx %>_<%= iIndex %>_<%= indIndex %>"
                       data-criteria-index="<%= iIndex %>"
                       id="project_<%= idx %>_<%= prindx %>_<%= iIndex %>_<%= indIndex %>_<%= mark.replace('.', '_') %>">

                <label for="project_<%= idx %>_<%= prindx %>_<%= iIndex %>_<%= indIndex %>_<%= mark.replace('.', '_') %>">
                    <%= mark %>
                </label>

            <% } %>
        </div>

        <input type="number"
               step="0.5"
               min="0"
               max="<%= ind.indicatorsMarks %>"
               class="mark-input project-input"
               data-project="<%= idx %>-<%= prindx %>"
               data-criteria-index="<%= iIndex %>"
               data-max-mark="<%= ind.indicatorsMarks %>"
               id="project_input_<%= idx %>_<%= prindx %>_<%= iIndex %>_<%= indIndex %>"
               placeholder="0-<%= ind.indicatorsMarks %>">
    </div>
</td>
                                        </tr>
                                      <% }) %>
                                      <% iIndex++ %>
                                    <% }) %>
                                  <% }) %>
                                <% }) %>
                              </tbody>
                            </table>
                          </div>
                          <div class="total-display">
                            <label>Total Project Marks</label>
                            <input type="number" name="unit[<%= idx %>][projectWorks][<%= prindx %>][projectMarks]" 
                                   value="0" id="project-total-<%= idx %>-<%= prindx %>" 
                                   class="form-control" readonly>
                          </div>
                        </div>
                      </div>
                    <% }) %>
                  </div>
                </div>
              <% }) %>
            <% }) %>
          </div>
        </div>

        <button type="submit" class="submit-btn">Submit Evaluation Form</button>
      </form>
    </div>
  </div>

  <input type="number" value="<%=workingDays%>" id="totalWorkingDays" style="display:none;">
  <input type="hidden" value="<%=subjectMarksData.theory%>" id="fullMarks">

  <script>
    // Populating terminal
    const terminal = window.location.search;
    const terminalParams = new URLSearchParams(terminal);
    const terminalValue = terminalParams.get('terminal');
    document.querySelector('#terminal').value = terminalValue;

    function calculateTerminalMarks() {
      const terminal = window.location.search;
      const terminalParams = new URLSearchParams(terminal);
      const terminalValue = terminalParams.get('terminal');
  
      if(terminalValue != "FINAL") {
        const theoryMarks = parseFloat(document.getElementById('theoryMarks').value);
        const fullMarks = parseFloat(document.getElementById('fullMarks').value);
        const studentClass = "<%= studentClass %>";
        if(isNaN(theoryMarks) || isNaN(fullMarks) || fullMarks === 0) {
          document.getElementById('obtainedconvertedmarks').value = '';
          return;
        }
        if(studentClass>8) {
          document.getElementById('obtainedconvertedmarks').value = ((theoryMarks/fullMarks)*6).toFixed(2);
        } else {
          document.getElementById('obtainedconvertedmarks').value = ((theoryMarks/fullMarks)*10).toFixed(2);
        }
      }
    }

    function calculateAttendanceMarks() {
      const attendanceDays = parseInt(document.getElementById('attendanceDays').value, 10) || 0;
      const studentClass = "<%= studentClass %>";
      const totalWorkingDays = document.getElementById('totalWorkingDays').value || 0;
      const attendancePercentage = (attendanceDays / totalWorkingDays) * 100;

      if(attendancePercentage >=90 && attendancePercentage <=100) {
        document.getElementById('attendanceMarks').value = studentClass>8 ? 1 : 2;
      } else if(attendancePercentage >= 85 && attendancePercentage < 90) {
        document.getElementById('attendanceMarks').value = studentClass>8 ? 0.75 : 1.5;
      } else if(attendancePercentage >= 80 && attendancePercentage < 85) {
        document.getElementById('attendanceMarks').value = studentClass>8 ? 0.5 : 1;
      } else if(attendancePercentage >=75 && attendancePercentage < 80) {
        document.getElementById('attendanceMarks').value = studentClass>8 ? 0.25 : 0.5;
      } else {
        document.getElementById('attendanceMarks').value = 0;
      }
    }

    // Update practical total
    function updatePracticalTotal(group) {
      let totalInput = document.getElementById("practical-total-" + group);
      if (!totalInput) return;
      
      let checked = document.querySelectorAll(`input[data-practical="${group}"]:checked`);
      let total = Array.from(checked).reduce((sum, r) => {
        let marks = parseFloat(r.value.split('||')[0]) || 0;
        return sum + marks;
      }, 0);
      
      totalInput.value = total;
    }

    // Update project total
    function updateProjectTotal(group) {
      let totalInput = document.getElementById("project-total-" + group);
      if (!totalInput) return;
      
      let checked = document.querySelectorAll(`input[data-project="${group}"]:checked`);
      let total = Array.from(checked).reduce((sum, r) => {
        let marks = parseFloat(r.value.split('||')[0]) || 0;
        return sum + marks;
      }, 0);
      
      totalInput.value = total;
    }

    // Handle practical radio change
    document.querySelectorAll(".marks-radio").forEach(radio => {
      radio.addEventListener("change", function(e) {
        let inputId = this.dataset.inputId;
        let input = document.getElementById(inputId);
        if (input) {
          let marks = this.value.split('||')[0];
          input.value = marks;
          input.classList.add('has-value');
          input.classList.remove('saved-value');
        }
        updatePracticalTotal(this.dataset.practical);
      });
    });

    // Handle project radio change
    document.querySelectorAll(".marks-radio-project").forEach(radio => {
      radio.addEventListener("change", function(e) {
        let inputId = this.dataset.inputId;
        let input = document.getElementById(inputId);
        if (input) {
          let marks = this.value.split('||')[0];
          input.value = marks;
          input.classList.add('has-value');
          input.classList.remove('saved-value');
        }
        updateProjectTotal(this.dataset.project);
      });
    });

    // Handle practical manual input
    document.querySelectorAll(".practical-input").forEach(input => {
      input.addEventListener("input", function(e) {
        let radioId = this.dataset.radioId;
        let radio = document.getElementById(radioId);
        let value = this.value.trim();
        let maxMark = parseFloat(this.dataset.maxMark) || 0;
        
        // Validate input
        if (value) {
          let numValue = parseFloat(value);
          if (!isNaN(numValue) && numValue >= 0 && numValue <= maxMark) {
            if (radio) {
              let parts = radio.value.split('||');
              parts[0] = value;
              radio.value = parts.join('||');
              radio.checked = true;
              this.classList.add('has-value');
              this.classList.remove('saved-value');
            }
          } else {
            // Invalid value - uncheck radio
            if (radio) radio.checked = false;
            this.classList.remove('has-value');
            this.classList.remove('saved-value');
          }
        } else {
          // Empty value - uncheck radio
          if (radio) radio.checked = false;
          this.classList.remove('has-value');
          this.classList.remove('saved-value');
        }
        updatePracticalTotal(this.dataset.practical);
      });

      input.addEventListener("blur", function(e) {
        let value = this.value.trim();
        let maxMark = parseFloat(this.dataset.maxMark) || 0;
        
        if (value) {
          let numValue = parseFloat(value);
          if (!isNaN(numValue) && numValue >= 0 && numValue <= maxMark) {
            let radioId = this.dataset.radioId;
            let radio = document.getElementById(radioId);
            if (radio) {
              let parts = radio.value.split('||');
              parts[0] = value;
              radio.value = parts.join('||');
              radio.checked = true;
              this.classList.add('has-value');
              this.classList.remove('saved-value');
            }
          }
        }
        updatePracticalTotal(this.dataset.practical);
      });
    });

    // Handle project manual input
    document.querySelectorAll(".project-input").forEach(input => {
      input.addEventListener("input", function(e) {
        let radioId = this.dataset.radioId;
        let radio = document.getElementById(radioId);
        let value = this.value.trim();
        let maxMark = parseFloat(this.dataset.maxMark) || 0;
        
        if (value) {
          let numValue = parseFloat(value);
          if (!isNaN(numValue) && numValue >= 0 && numValue <= maxMark) {
            if (radio) {
              let parts = radio.value.split('||');
              parts[0] = value;
              radio.value = parts.join('||');
              radio.checked = true;
              this.classList.add('has-value');
              this.classList.remove('saved-value');
            }
          } else {
            if (radio) radio.checked = false;
            this.classList.remove('has-value');
            this.classList.remove('saved-value');
          }
        } else {
          if (radio) radio.checked = false;
          this.classList.remove('has-value');
          this.classList.remove('saved-value');
        }
        updateProjectTotal(this.dataset.project);
      });

      input.addEventListener("blur", function(e) {
        let value = this.value.trim();
        let maxMark = parseFloat(this.dataset.maxMark) || 0;
        
        if (value) {
          let numValue = parseFloat(value);
          if (!isNaN(numValue) && numValue >= 0 && numValue <= maxMark) {
            let radioId = this.dataset.radioId;
            let radio = document.getElementById(radioId);
            if (radio) {
              let parts = radio.value.split('||');
              parts[0] = value;
              radio.value = parts.join('||');
              radio.checked = true;
              this.classList.add('has-value');
              this.classList.remove('saved-value');
            }
          }
        }
        updateProjectTotal(this.dataset.project);
      });
    });

    // Function to set input value and highlight it
    function setPracticalInputValue(radio, mark) {
    let inputId = radio.dataset.inputId;
    let input = document.getElementById(inputId);
    if (input) {
        input.value = mark || radio.value.split('||')[0];
        input.classList.add('has-value');
        input.classList.add('saved-value');
    }
}

function setProjectInputValue(radio, mark) {
    let inputId = radio.dataset.inputId;
    let input = document.getElementById(inputId);
    if (input) {
        input.value = mark || radio.value.split('||')[0];
        input.classList.add('has-value');
        input.classList.add('saved-value');
    }
}

async function fill(roll, name, reg) {
    const buttons = document.querySelectorAll("[id^='studentData']");
    buttons.forEach(btn => {
        btn.style.backgroundColor = 'white';
        btn.style.color = 'black';
    });
    const clickedBtn = document.getElementById('studentData' + roll);
    clickedBtn.style.backgroundColor = 'darkblue';
    clickedBtn.style.color = 'white';
    document.getElementById('roll').value = roll;
    document.getElementById('name').value = name;
    document.querySelector('input[name="reg"]').value = reg;

    if (roll && name && reg) {
        fillAttendance();
        const response = await fetch(`/getPracticalData?roll=${roll}&reg=${reg}&studentClass=<%= studentClass %>&section=<%= section %>&subject=<%= subject %>&terminal=<%= terminal %>`);
        const practicalData = await response.json();
        console.log("Fetched data:", practicalData);

        if (practicalData.participationMarks) {
            document.querySelector(`input[name="participationMarks"][value="${practicalData.participationMarks}"]`).checked = true;
        } else {
            document.querySelectorAll(`input[name="participationMarks"]`).forEach(radio => radio.checked = false);
        }

        if (practicalData.totalAttendance) {
            document.getElementById('attendanceDays').value = practicalData.totalAttendance || 0;
            calculateAttendanceMarks();
        } else {
            document.getElementById('attendanceDays').value = '';
            document.getElementById('attendanceMarks').value = '';
        }

        if (practicalData.theoryMarks) {
            document.getElementById('theoryMarks').value = practicalData.theoryMarks;
            calculateTerminalMarks();
        } else {
            document.getElementById('theoryMarks').value = '';
            document.getElementById('obtainedconvertedmarks').value = '';
        }

        // Clear all inputs first
        document.querySelectorAll('.practical-input, .project-input').forEach(input => {
            input.value = '';
            input.classList.remove('has-value', 'saved-value');
        });

        // Clear all radios
        document.querySelectorAll('.marks-radio, .marks-radio-project').forEach(radio => {
            radio.checked = false;
        });

        // Populate practical marks
        if (practicalData.unit && Array.isArray(practicalData.unit)) {
            practicalData.unit.forEach((unit, unitIdx) => {
                if (unit.practicals && Array.isArray(unit.practicals)) {
                    unit.practicals.forEach((practical, practicalIdx) => {
                        if (practical.criteria && Array.isArray(practical.criteria)) {
                            // Get all inputs for this practical
                            let allInputs = document.querySelectorAll(`.practical-input[data-practical="${unitIdx}-${practicalIdx}"]`);
                            
                            // Match criteria with inputs by index
                            practical.criteria.forEach((c, criteriaIdx) => {
                                if (criteriaIdx < allInputs.length) {
                                    let mark = c.practicalIndicatorMarks ? c.practicalIndicatorMarks.toString().trim() : '';
                                    let input = allInputs[criteriaIdx];
                                    
                                    if (mark && input) {
                                        // Set input value
                                        input.value = mark;
                                        input.classList.add('has-value');
                                        input.classList.add('saved-value');
                                        
                                        // Find and check matching radio
                         const radios = document.querySelectorAll(
    `.marks-radio[data-practical="${unitIdx}-${practicalIdx}"]`
);
console.log("Radios for practical:", radios);

radios.forEach(radio => {
    const radioMark = radio.value.split("||")[0].trim();
    radio.checked = Number(radioMark) === Number(mark);
});
                                    }
                                }
                            });
                        }
                        updatePracticalTotal(`${unitIdx}-${practicalIdx}`);
                    });
                }

                if (unit.projectWorks && Array.isArray(unit.projectWorks)) {
                    unit.projectWorks.forEach((project, projectIdx) => {
                        if (project.criteria && Array.isArray(project.criteria)) {
                            let allInputs = document.querySelectorAll(`.project-input[data-project="${unitIdx}-${projectIdx}"]`);
                            
                            project.criteria.forEach((c, criteriaIdx) => {
                                if (criteriaIdx < allInputs.length) {
                                    let mark = c.projectIndicatorMarks ? c.projectIndicatorMarks.toString().trim() : '';
                                    let input = allInputs[criteriaIdx];
                                    
                                    if (mark && input) {
                                        input.value = mark;
                                        input.classList.add('has-value');
                                        input.classList.add('saved-value');
                                        
                                      const radios = document.querySelectorAll(
    `.marks-radio[data-project="${unitIdx}-${projectIdx}"]`
);
console.log("Radios for project:", radios);

radios.forEach(radio => {
    const radioMark = radio.value.split("||")[0].trim();
    radio.checked = Number(radioMark) === Number(mark);
});
                                    }
                                }
                            });
                        }
                        updateProjectTotal(`${unitIdx}-${projectIdx}`);
                    });
                }
            });
        }
    } else {
        // Clear form if no data
        document.getElementById('attendanceDays').value = '';
        document.getElementById('attendanceMarks').value = '';
        document.getElementById('theoryMarks').value = '';
        document.getElementById('obtainedconvertedmarks').value = '';

        document.querySelectorAll('.practical-input, .project-input').forEach(input => {
            input.value = '';
            input.classList.remove('has-value', 'saved-value');
        });

        document.querySelectorAll('.marks-radio, .marks-radio-project').forEach(radio => {
            radio.checked = false;
        });

        ScienceData.forEach((item) => {
            item.units.forEach((unit, unitIdx) => {
                unit.practicals.forEach((practical, practicalIdx) => {
                    document.getElementById(`practical-total-${unitIdx}-${practicalIdx}`).value = '0';
                });
                unit.projectWorks.forEach((project, projectIdx) => {
                    document.getElementById(`project-total-${unitIdx}-${projectIdx}`).value = '0';
                });
            });
        });
    }
}
  </script>

  <input type="text" id="attendanceData" value='<%= JSON.stringify(attendanceData) %>' style="display:none;">
  <input type="text" id="marksheetSetting" value="<%=JSON.stringify(marksheetSetting)%>" style="display:none;">
  
  <script>
    function fillAttendance() {
      const roll = document.getElementById('roll').value;
      const name = document.getElementById('name').value;
      const attendanceData = JSON.parse(document.getElementById('attendanceData').value);
      const marksheetSetting = JSON.parse(document.getElementById('marksheetSetting').value);
      const terminalname = document.getElementById('terminal').value;
      
      if(roll && name) {
        attendanceData.forEach(record => {
          if(record.roll === roll.toString()) {
            if(terminalname===marksheetSetting[0].terminals[0].name) {
              document.getElementById('attendanceDays').value = record.firstTerm || 0;
              calculateAttendanceMarks();
            } else if (terminalname===marksheetSetting[0].terminals[1].name) {
              document.getElementById('attendanceDays').value = record.secondTerm || 0;
              calculateAttendanceMarks();
            } else if (terminalname===marksheetSetting[0].terminals[2].name) {
              document.getElementById('attendanceDays').value = record.thirdTerm || 0;
              calculateAttendanceMarks();
            } else if (terminalname===marksheetSetting[0].terminals[3].name) {
              document.getElementById('attendanceDays').value = record.finalTerm || 0;
              calculateAttendanceMarks();
            }
          }
        });
      }
    }

    const ScienceData = <%- JSON.stringify(ScienceData) %>;

    function clearForm() {
      document.getElementById('theoryMarks').value = '';
      document.getElementById('obtainedconvertedmarks').value = '';
      document.querySelectorAll('input[type="radio"]').forEach(radio => {
        radio.checked = false;
      });
      document.querySelectorAll('.practical-input, .project-input').forEach(input => {
        input.value = '';
        input.classList.remove('has-value', 'saved-value');
      });
      document.querySelectorAll('[id^="practical-total-"]').forEach(el => el.value = '0');
      document.querySelectorAll('[id^="project-total-"]').forEach(el => el.value = '0');
      console.log("Form cleared.");
    }
  </script>
</body>
</html>